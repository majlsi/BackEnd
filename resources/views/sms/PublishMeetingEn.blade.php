You have been invited to meeting {{$meeting}}.
@if($zoomJoinUrl)
To join zoom meeting use this link: {{$zoomJoinUrl}}
@endif
@if($microsoftTeamsJoinUrl)
To join Microsoft Teams meeting use this link: {{$microsoftTeamsJoinUrl}}
@endif